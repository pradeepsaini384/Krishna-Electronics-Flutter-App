name: my_browser_app
description: A Flutter app that acts as a browser and loads a single website.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
